include ("${EXTERNALS_LIB_LOCATION}/CMake-Register_External_Lib.txt")

set (JEMALLOC_ARCHIVE "${JEMALLOC_BUILTIN_LOCATION}/lib/libjemalloc_pic.a")

register_external_lib (jemalloc ${JEMALLOC_BUILTIN_LOCATION} ${JEMALLOC_ARCHIVE})

set (CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -I${JEMALLOC_BUILTIN_LOCATION} -D_BUILT_IN_JEMALLOC")
set (CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -I${JEMALLOC_BUILTIN_LOCATION} -D_BUILT_IN_JEMALLOC")
